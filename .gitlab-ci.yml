variables:
    # https://gitlab.com/gitlab-org/gitlab-runner/-/issues/1809
    #FRONTEND_PROJECT_NAME: "Wally.RomMaster"
    BACKEND_PROJECT_NAME: "Wally.RomMaster"
    #
    CI_DEBUG_TRACE: "false"
    CI_REGISTRY_USER: ${REGISTRY_USER}
    CI_REGISTRY_PASSWORD: ${REGISTRY_PASSWORD}

stages:
    - build
    - test
    - publish
    - deploy

include:
    - local: .gitlab/ci/backend.gitlab-ci.yml
    #- local: .gitlab/ci/frontend.gitlab-ci.yml
    - local: .gitlab/ci/deploy.gitlab-ci.yml
